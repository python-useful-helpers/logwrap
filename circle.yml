machine:
  python:
    version: 3.5.2

dependencies:
  override:
    - sudo pip install -U virtualenv
    - sudo pip install -U setuptools
    - sudo pip install -U tox

test:
  override:
    - tox -e docs